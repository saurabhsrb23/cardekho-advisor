# ── Anthropic ─────────────────────────────────────────────────────────────────
# Get your key at https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# ── Database ──────────────────────────────────────────────────────────────────
# Development — SQLite (zero config, single file)
DATABASE_URL="file:./prisma/dev.db"

# Production — Vercel Postgres / Neon (swap provider to "postgresql" in schema.prisma)
# DATABASE_URL="postgresql://user:password@host:5432/cardekho?sslmode=require"
